services:
  db:
    image: pgvector/pgvector:pg16
    container_name: hhs_db
    env_file:
      - .env
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - hhs_db_data:/var/lib/postgresql/data
    restart: unless-stopped
volumes:
  hhs_db_data:
